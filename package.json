{
  "name": "input-math",
  "version": "1.0.2",
  "description": "jQuery plugin to evaluate the written math expression on input[type=text] every Enter, Just like Blender or Gimp",
  "main": "lib/inputMath.js",
  "scripts": {
    "test": "mocha",
    "build": "node_modules/.bin/babel src --out-dir lib",
    "build-test": "npm run build && npm test"
  },
  "author": "Levhita <levhita@gmail.com>",
  "license": "ISC",
  "repository": {
    "type": "git",
    "url": "https://github.com/levhita/inputMath.git"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "jquery": "^3.3.1",
    "jsdom": "^11.11.0",
    "mocha": "^5.2.0"
  },
  "dependencies": {
    "babel-preset-env": "^1.7.0"
  }
}
